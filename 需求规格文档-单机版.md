# 彩票管理安卓应用 - 需求规格文档（单机版）

## 1. 项目概述

### 1.1 产品名称
**彩票智查** - 本地版

### 1.2 产品定位
纯本地运行的安卓应用，无需注册、无需联网，所有数据存储在手机本地，保护隐私。

### 1.3 核心价值
- ✅ 完全离线，无网络也可用
- ✅ 无账号体系，隐私绝对安全
- ✅ 拍照识别号码（本地OCR）
- ✅ 自动判断中奖（内置开奖数据）
- ✅ 智能预测（本地算法）
- ✅ 年度统计ROI

---

## 2. 功能需求（精简版）

### 2.1 彩票识别（拍照/手动输入）
- **拍照识别**：调用相机拍照，本地OCR识别双色球/大乐透号码
- **手动输入**：用户手动输入期号和号码
- **支持类型**：双色球（6+1）、大乐透（5+2）

### 2.2 开奖查询与中奖判断
- **内置开奖数据**：随应用打包历史开奖数据（CSV/JSON）
- **自动匹配**：识别后自动查询本地理奖数据，判断中奖
- **中奖等级**：根据规则计算等级和奖金
- **结果展示**：高亮匹配号码，显示奖金

### 2.3 记录管理
- **保存记录**：期号、号码、开奖结果、中奖信息
- **查看历史**：列表展示，按时间倒序
- **编辑/删除**：支持修改和删除
- **筛选**：按彩票类型、中奖状态筛选

### 2.4 智能预测
- **本地算法**：5种策略（热冷、奇偶、邻号、余数、随机）
- **多组推荐**：用户选择生成组数（1-10）
- **分析统计**：显示冷热号、奇偶分布等

### 2.5 统计报表
- **年度统计**：本年度花费、中奖金额、ROI
- **中奖分布**：各奖级次数和奖金
- **趋势图表**：月度柱状图（可选）

### 2.6 数据管理
- **备份导出**：JSON 格式导出到文件
- **恢复导入**：从 JSON 文件导入
- **清空数据**：一键清空所有记录

---

## 3. 非功能需求

- **性能**：OCR识别 < 3秒，界面流畅
- **兼容性**：Android 8.0+ (API 26)
- **存储**：数据占用 < 10MB（历史1000期）
- **离线**：完全离线运行，无需网络

---

## 4. 技术方案

### 4.1 前端（Android）
- **语言**：Kotlin
- **UI**：Jetpack Compose + Material 3
- **架构**：MVVM + Repository 模式
- **数据库**：Room (SQLite)
- **相机**：CameraX
- **OCR**：Tesseract.js 或 ML Kit（本地）

### 4.2 数据
- **开奖数据**：打包在 assets 文件夹的 JSON（2020-2025）
- **用户数据**：Room 数据库（本地）
- **备份文件**：存储在 Downloads 目录

---

## 5. 数据模型

### 5.1 LotteryRecord（投注记录）
```kotlin
@Entity(tableName = "records")
data class LotteryRecord(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val period: String,              // 期号 2024050
    val type: String,                // "double_color_ball" | "super_lotto"
    val redBalls: List<Int>,         // 红球列表
    val blueBall: Int,               // 蓝球（双色球）或 null
    val frontBalls: List<Int>?,      // 前区（大乐透）
    val backBalls: List<Int>?,       // 后区（大乐透）
    val drawRedBalls: List<Int>?,    // 开奖红球
    val drawBlueBall: Int?,          // 开奖蓝球
    val drawFrontBalls: List<Int>?,  // 开奖前区
    val drawBackBalls: List<Int>?,   // 开奖后区
    val matchedRedCount: Int = 0,
    val matchedBlue: Boolean = false,
    val matchedFrontCount: Int = 0,
    val matchedBackCount: Int = 0,
    val prizeLevel: String? = null,
    val prizeAmount: Double = 0.0,
    val betAmount: Double = 2.0,     // 投注金额默认2元
    val createdAt: Long = System.currentTimeMillis()
)
```

### 5.2 DrawResult（开奖结果）
```kotlin
@Entity(tableName = "draws")
data class DrawResult(
    @PrimaryKey val period: String,
    val type: String,
    val drawDate: Long,
    val redBalls: String,  // "01,05,12,19,23,29"
    val blueBall: Int,
    val frontBalls: String? = null,  // 大乐透前区
    val backBalls: String? = null    // 大乐透后区
)
```

---

## 6. 页面清单

1. **MainActivity** - 主入口（底部导航）
2. **HomeFragment** - 首页（统计+最近记录）
3. **CameraFragment** - 拍照识别
4. **RecordsFragment** - 记录列表
5. **RecordDetailFragment** - 记录详情
6. **PredictionFragment** - 智能预测
7. **StatsFragment** - 统计报表
8. **SettingsFragment** - 设置（备份/清空）

---

## 7. 开发里程碑

| 阶段 | 任务 | 工时 | 交付物 |
|------|------|------|--------|
| 1. 需求分析 | 编写需求文档 | 0.5天 | PRD |
| 2. 原型设计 | 低保真线框图 | 1天 | 原型文档 |
| 3. UI设计 | 高保真设计稿 | 2天 | 设计文件 |
| 4. 架构设计 | 技术方案、数据库设计 | 1天 | 设计文档 |
| 5. 编码开发 | Android开发 | 7-10天 | 可运行APK |
| 6. 测试 | 功能测试、兼容性测试 | 2天 | 测试报告 |
| 7. 发布 | 打包、上传GitHub | 0.5天 | Release |
| **总计** | | **14-17天** | |

---

## 8. 关键决策点

1. **OCR方案**：Tesseract.js（WebView）还是 ML Kit（原生）？
2. **开奖数据更新**：打包static，还是支持在线更新？
3. **图表库**：使用 MPAndroidChart 还是自定义Canvas？
4. **备份格式**：JSON（可读）还是 SQLite 副本（完整）？

---

## 9. 风险

- **OCR准确率**：彩票样式多样，需充分测试和人工校正
- **开奖数据维护**：需要定期更新数据库
- **兼容性**：不同厂商相机权限处理

---

## 10. 用户故事

### US-01: 记录投注
作为彩民，我可以拍照或手动输入彩票号码，保存投注记录，并自动查询开奖结果。

### US-02: 查看历史
作为彩民，我可以查看所有历史投注记录，筛选和搜索。

### US-03: 智能预测
作为彩民，我可以使用本地算法获取下期号码推荐。

### US-04: 统计分析
作为彩民，我可以查看年度花费、中奖金额和ROI。

---

**版本**: v1.0  
**日期**: 2025-06-20  
**作者**: 大龙虾 🦞
